package com.suggestion.service.domain;

/*
 * A domain logic object used to communicate
 * the results of actions called from one service to another
 * inside the domain logic
 */
public enum Response {
	SUCCESS, COMPLETED, NO_DATA, FAIL; 
}
